# Requirements traceability

This matrix connects the normative contracts in `docs/architecture.md` to their
implementation and verification evidence. `Planned` means the contract is frozen
but the named artifact does not yet exist. A requirement cannot close with a
blank evidence category; justified exclusions must be written explicitly.

| Requirement | RTL | Directed test | Assertion/formal | Coverage | Status |
|---|---|---|---|---|---|
| Completion commits exactly once or is explicitly cancelled | Completion fabric and elastic writeback queue | Directed queue tests, backpressure, fatal recovery, and mid-flight clear differential tests | Stable handshakes plus exhaustive writeback side-effect proof | Completion source and both stall states closed | Verified for all pre-memory completion sources |
| Round-robin scheduler services a continuously eligible warp within four accepted grants | `rtl/control/round_robin_arbiter.sv`, `rtl/core/simt_core.sv` | `tb/unit/tb_round_robin_arbiter.sv`, `tb/integration/tb_four_warp_core.sv` | One-hot, requested-grant, stalled stability, and accepted-grant wait proven to depth 12 with induction | All four warps issue and retire; ready-count bins pending | Verified for the shared four-request arbiter; integrated assumptions remain reviewed |
| Instruction fetch uses one shared SRAM-compatible port and stable per-warp buffers | `rtl/frontend/warp_instruction_frontend.sv`, `rtl/core/simt_core.sv` | Frontend unit test plus randomized fetch-backpressure differential test | One-hot/request validity, stable-response, buffer-PC, and request-bound assertions | All four warps, prolonged response stalls, refill, redirect, flush | Verified with behavioral adapter and qualified IHP macro views |
| Four-warp architectural behavior agrees with an independent reference | `model/emulator/multi_warp_emulator.cpp`, `rtl/core/simt_core.sv` | 21-run, three-seed UVM matrix plus directed arithmetic/divergence | Unique epoch/warp/sequence keys, first-mismatch comparator, and sequence-continuity scoreboard | 43/43 approved pre-memory risk bins | Verified for the complete pre-memory ISA and lifecycle boundary |
| Round-robin writeback services a continuously nonempty source within three accepted commits | `rtl/execute/completion_arbiter.sv` | Completion-arbiter collision test and randomized integrated stalls | Shared three-request safety, stability, and wait proven to depth 12 with induction | Both implemented sources and collisions exercised | Verified for pre-memory sources |
| GPR pending state sets only on accepted issue and clears only on matching commit | `rtl/control/dependency_scoreboard.sv` | `tb/unit/tb_dependency_scoreboard.sv` | Issue-ready assertion; exhaustive matching properties pending | Opcode × dependency pending | Partial: component RAW/WAW and tag matching verified |
| Predicate pending state has no forwarding and clears only on matching commit | Scoreboard and predicate register file | Scoreboard RAW/WAW plus register-file no-forwarding tests | Issue-ready and committed-state checks; exhaustive formal pending | Predicate producer × consumer pending | Partial: component behavior verified |
| Stale epochs have no architectural or memory side effects | `rtl/execute/architectural_writeback.sv`, `rtl/memory/memory_subsystem.sv` | ALU writeback plus epoch clear/relaunch differential tests | Exhaustive writeback stale-cancellation and side-effect proof; memory proof pending | Stale cancellation and epoch-1 relaunch | Partial: clear cancels tagged trackers; adversarial response proof remains |
| Epoch reuse is permitted only after full quiescence | Epoch/quiescence controller | Clear, drain, wrap-directed test | Quiescence completeness | Quiescence blocker × launch | Planned |
| Fatal faults immediately block issue/commit and use a dedicated sticky path | `rtl/control/fatal_fault_controller.sv` and commit gating | ALU priority, lifecycle, fault recovery, and mid-flight clear tests | Sticky-fault assertions and exhaustive writeback fatal-suppression proof | Fault suppression with empty and outstanding work | Verified for the pre-memory machine |
| Stores within one warp instruction execute in ascending lane order | `rtl/memory/banked_vector_memory.sv` | Same-bank, same-address, and distinct-bank timing tests | Six-cycle bank-engine safety proof plus service selection assertions | Full participation with conflict and alias cases | Verified directed and bounded; random conflict coverage remains |
| Data SRAM wrapper preserves word/byte mapping and BIST ownership | `rtl/memory/data_sram_bank_adapter.sv`, both vector bank engines | Behavioral-versus-IHP-model adapter equivalence, bank-engine and integrated memory tests | Stable-response, BIST-exclusion, and bank-engine bounded properties | Full/partial write, low/high half, stall, ownership | Verified and integrated; 17-macro mapped netlist and legal trial placement pass |
| A younger same-warp load observes the completed older store | Memory subsystem and core warp-busy gate | End-to-end store/load/RAW test, shared reduction, and constrained-random memory differential sequence | One tracker per warp assertions | Load/store and general/shared bins closed across five random seeds | Verified directed and licensed differential |
| Barrier release waits for all required warps and older memory completion | Core barrier state, memory-busy gate, and parameterized deadlock watchdog | Four-warp barrier, reduction, and architectural missing-warp fault | Release precondition and waiting-warp issue-suppression assertions | Four arrivals and missing-warp deadlock | Verified directed; randomized arrival coverage remains |
| Two-entry completion queues never overwrite, duplicate, or lose payloads | `rtl/execute/completion_queue.sv`, ALU and multiplier instances | Queue, multiplier, backpressure, and full-exchange tests | Occupancy, valid-tag, stability, and exact commit-count assertions | Empty, partial, full, simultaneous dequeue/enqueue, flush | Verified; FIFO ordering and capacity mutations detected |
| Four memory trackers are system-wide with at most one per warp | `rtl/memory/memory_subsystem.sv` | Fifth-operation and same-warp rejection test | Allocation uniqueness assertions and detected duplicate-allocation mutation | Occupancy four and rejected fifth request | Verified directed and mutation-tested |
| Instruction writes while busy are suppressed and fault deterministically | Core programming gate, fatal controller, and ASIC Wishbone host path | Lifecycle and ASIC host-controller tests | SRAM collision assertion; end-to-end bus/core properties pending | Bus state × busy write pending | Partial: core and host components verified independently |
| GPR replicas remain consistent after every accepted masked write | Vector register file | Full and partial-mask write tests | Replica consistency assertions | Warp × register × lane mask | Verified at component level |
| Predicate reads expose committed state only | Predicate register file | No-forwarding test | Committed-state read checks | Address × masked write | Verified at component level |
| Same-cycle events obey reset, clear, fatal, commit, then progress priority | Global control and commit gating | Fifteen fatal-source pairs, reset/fault, clear/fault, sticky replacement, and fatal/writeback tests | Sticky-fault and writeback suppression assertions | Fatal pair matrix closed; broader host/core pairs remain | Partial: fatal subpriority and writeback boundary verified |
| Vector memory faults are instruction-atomic | Bank-engine prevalidation | Misaligned and out-of-range active-lane tests | No bank service before complete validation | Load/store and inactive-lane exception | Verified directed |
| Kernel done requires all warps exited and complete machine drain | `rtl/core/simt_core.sv` | Four-warp, lifecycle, memory, barrier, backpressure, cancellation, and recovery tests | Drain precondition assertions and detected early-done mutation | ALU/multiplier/memory/writeback blockers exercised | Verified directed for integrated sources |
| EXIT deactivates only its effective lane mask | Single-warp lane-exit logic complete; divergent controller planned | Full-mask and predicated partial-mask EXIT tests | Deferred-path properties pending | Exit mask × stack state pending | Partial: nondivergent lane exit verified |
| Divergent branches execute taken and deferred masks before reconvergence | `rtl/core/simt_core.sv` per-warp SIMT stacks | Four-warp divergence and structured-control differential tests | Stack-depth/stability/fault assertions plus detected mask-swap and stack-wrap mutations | Taken/deferred, depth-two nested, four-warp simultaneous, overflow and underflow | Verified for the pre-memory release |
| Sequence matching uses 16-bit sequence plus epoch and warp | Issue tags and all delayed paths | Wrap-near-boundary tag test | No mismatched scoreboard clear/commit | Source × tag fields | Planned |
| Architectural counters wrap at 64 bits and diagnostic counters saturate at 32 bits | Performance counters | Boundary and saturation tests | Width/update/saturation properties | Counter class × boundary | Planned |
| Debug uses a bounded coherent snapshot without a full GPR mux | Debug snapshot unit | Live and fault snapshot tests | Snapshot stability and field coherence | Trigger × captured structure | Planned |
| Ungated baseline and any later clock gating preserve behavior and scan visibility | ASIC test ownership and scan stitch boundary | Host test-mode ownership and reset tests | ASIC top strict lint and pre-DFT synthesis equivalence; post-insertion scan properties pending | Functional/test/scan modes constrained | Partial: ungated static signoff closed; scan insertion pending |
| Host control and SRAM maintenance preserve ownership and debug bounds | Wishbone controller, bounded snapshot, quiescent bank-engine maintenance ports | Directed host checks; 114-check ASIC-top diagnostic; bank and subsystem maintenance readback | Test/reset ownership assertions and static release-contract check | Busy/error, both spaces, snapshot, breadcrumbs, production injection disable | Verified; host/SRAM release gate closed |

The matrix expands as implementation proceeds. Detailed report paths, test names,
property identifiers, coverage percentages, owners, and waiver links replace the
generic artifact names before verification closure.

Before shared-memory RTL begins, the roadmap additionally requires one
authoritative parameterized core, an SRAM-compatible buffered instruction
frontend, a four-warp reference model, early synthesis/floorplanning evidence,
randomized backpressure, selected bounded proofs, and an initial mutation report.
These are architecture-stabilization gates rather than deferred signoff polish.

The optional post-baseline FP32 add/multiply extension has no baseline
traceability credit. If activated, it requires a frozen numerical contract,
independent bit-accurate reference, IEEE edge-case and random differential tests,
pipeline/backpressure assertions, four-source arbitration evidence, and new
area/timing/power reports before it may be described as complete.
